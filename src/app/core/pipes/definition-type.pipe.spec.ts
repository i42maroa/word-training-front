import { DefinitionTypePipe } from './definition-type.pipe';

describe('DefinitionTypePipe', () => {
  it('create an instance', () => {
    const pipe = new DefinitionTypePipe();
    expect(pipe).toBeTruthy();
  });
});
